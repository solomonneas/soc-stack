# TheHive + Cortex Configuration
# Copy to .env and edit before deploying.

# TheHive secret key (generate a random one)
THEHIVE_SECRET=change-me-to-something-random

# Passwords for admin accounts (set by setup.sh)
THEHIVE_ADMIN_PASSWORD=changeme
CORTEX_ADMIN_PASSWORD=changeme

# Organization name for Cortex
CORTEX_ORG_NAME=Neas
